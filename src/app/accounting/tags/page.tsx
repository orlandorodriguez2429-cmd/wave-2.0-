import { ComingSoon } from '@/components/coming-soon';

export default function TagsPage() {
  return (
    <ComingSoon
      title="Tags"
      description="Custom tags for slicing transactions across categories (by project, location, etc.) aren't built yet — this needs a data-model addition to journal entries."
    />
  );
}
