import { ComingSoon } from '@/components/coming-soon';

export default function HireABookkeeperPage() {
  return (
    <ComingSoon
      title="Hire a Bookkeeper"
      description="On-demand bookkeeping service referrals aren't set up yet — this requires a staffed service this project doesn't provide. Invite your own accountant instead from Settings → Team & API."
    />
  );
}
