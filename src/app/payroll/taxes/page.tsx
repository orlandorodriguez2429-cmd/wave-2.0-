import { ComingSoon } from '@/components/coming-soon';

export default function PayrollTaxesPage() {
  return (
    <ComingSoon
      title="Payroll Taxes"
      description="Federal/state payroll tax withholding and deposits aren't built yet — this needs a licensed payroll-tax provider this project doesn't have. Sales tax (a different thing) is tracked under Tax filing → Sales Tax."
    />
  );
}
