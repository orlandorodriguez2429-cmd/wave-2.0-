import { ComingSoon } from '@/components/coming-soon';

export default function PayrollTransactionsPage() {
  return (
    <ComingSoon
      title="Payroll Transactions"
      description="Payroll runs (gross pay, withholding, net deposit) aren't built yet — this needs a payroll-tax provider and a real payroll-run engine this project doesn't have. Employees and Timesheets are ready as the data source once one is connected."
    />
  );
}
