import { ComingSoon } from '@/components/coming-soon';

export default function PerksPage() {
  return (
    <ComingSoon
      title="Perks"
      description="Partner discounts on business tools and services aren't set up yet — this requires real partner agreements this project doesn't have."
    />
  );
}
