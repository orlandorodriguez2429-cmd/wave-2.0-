import { ComingSoon } from '@/components/coming-soon';

export default function ReceiptsPage() {
  return (
    <ComingSoon
      title="Receipts"
      description="Snap or upload a receipt to auto-fill an expense. Receipt OCR needs a third-party integration this project hasn't wired up yet — for now, attach receipts directly from the Expenses page."
    />
  );
}
